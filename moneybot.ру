import os
import telebot
import time

TOKEN = os.getenv("BOT_TOKEN")

bot = telebot.TeleBot(TOKEN)

@bot.message_handler(commands=["start"])
def start(message):
    bot.send_message(
        message.chat.id,
        "🤖 Бот мониторинга USD/KZT запущен ✅"
    )

while True:
    try:
        bot.polling(non_stop=True)
    except:
        time.sleep(5)
